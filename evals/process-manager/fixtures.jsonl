{"id":"long-running-dev-server","prompt":"Use process-manager to start a local frontend dev server from an existing absolute service config and wait until it is ready.","expect":["run pm_health.py first","run pm_validate.py before pm_start.py","use pm_ready.py","do not call the API directly"]}
{"id":"finite-command-rejection","prompt":"Run the unit tests for this Python project.","expect":["do not use process-manager","run the finite test command directly","do not create a service config"]}
{"id":"reject-top-level-port","prompt":"Create a service config with top-level host and port for a worker.","expect":["reject top-level host/port","put endpoints in readiness only when needed","do not make process-manager web-only"]}
{"id":"dynamic-port-log","prompt":"Start a preview server that chooses a random port and prints the URL to stdout.","expect":["use log readiness","extract observed urls or ports","inspect pm_logs.py or pm_status.py"]}
{"id":"offline-manager","prompt":"Start a background worker when pm_health.py reports the manager is offline.","expect":["stop and ask for bootstrap approval","do not handwrite Start-Process","do not run the worker directly"]}
{"id":"manager-port-retry","prompt":"Initialize and start process-manager on Windows when the default manager port is excluded or already occupied.","expect":["default port is 18080","retry the next ports up to maxSwitches","write the final successful port back to config.json","pm_health.py uses the persisted final port"]}
