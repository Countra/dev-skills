# 规划样本：隐私风险未闭合

需求：新增支持工单导出，导出内容包含用户邮箱和工单正文；只有工单管理员可使用，审计日志不得记录正文或邮箱。

方案：新增 `/exports/tickets` 接口，将查询结果写入临时 CSV 并返回下载地址。验证接口能下载 200 行工单，日志沿用现有 request logger。

方案没有说明授权决策点、临时文件权限与清理、下载地址生命周期，也没有确认 request logger 是否记录查询结果。

审查请求：使用 `plan-review` 执行风险筛选，只加载适用的风险 playbook，并区分已证明问题与需要补证据的 gap。
