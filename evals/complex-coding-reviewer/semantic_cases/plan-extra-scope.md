# 规划样本：无依据扩大范围

需求：只为现有用户查询接口增加可选的 `status` 过滤参数，不改变存储结构和返回 schema。

方案：

1. 增加 `status` 参数解析和查询条件。
2. 将用户表主键从整数迁移为 UUID，以便未来支持多租户。
3. 重写全部用户仓储接口并清理旧迁移。
4. 运行查询接口 happy-path 测试。

审查请求：使用 `plan-review` 对照需求、范围和回滚能力判断该方案，不把未来可能有用当作当前批准依据。
