# 规划样本：依赖过期上下文

需求：使用当前项目锁定版本的队列 SDK 增加延迟消息。

方案依据：一份标注为 SDK 2.x 的本地设计摘录声称 `publish(message, delay_seconds=...)` 可直接设置延迟；项目 lockfile 当前锁定 4.x，但方案没有读取 4.x API、迁移说明或项目内调用方。

方案据此直接修改 producer，并把“SDK 支持延迟参数”列为已确认事实。

审查请求：使用 `plan-review` 判断当前证据能支持到什么程度；无法确认当前版本能力时明确 gap、owner 和关闭条件。
