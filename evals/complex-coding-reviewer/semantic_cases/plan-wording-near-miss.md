# 规划样本：措辞近似项

目标：为导出命令新增 `--format json`，保持默认文本输出不变。

实施：在现有参数解析器中增加枚举值，复用当前 serializer；补充默认输出与 JSON 输出的单元测试，并运行 CLI 回归。

验收：默认调用与基线逐字节一致；JSON 输出可解析且字段集合固定；未知格式返回现有参数错误。

回滚：移除枚举值和对应 serializer 路由，不涉及持久化数据。

项目规范没有规定标题必须使用“阶段”还是“步骤”。审查请求：使用 `plan-review` 判断是否存在交付前必须修复的问题，不要把措辞偏好当作行为缺陷。
