# Reviewer Scenarios

`semantic_cases/` 保留 plan/code clean、missing、extra、misunderstood、风险、stale context、framing bias、prompt injection 和 parent contamination 样本。

运行时观察应确认：结果 findings-first、定位到路径和行号、说明 coverage/gaps，不生成 JSON 审查制品；高风险独立审查不可用时不伪造独立性。
