interface:
  display_name: "Electron UI Verifier"
  short_description: "验证 Electron UI、诊断运行状态并沉淀可信证据"
  default_prompt: "Use $electron-ui-verifier to verify this Electron UI flow and return evidence-backed findings."
