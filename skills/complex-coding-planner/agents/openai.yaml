interface:
  display_name: "Complex Coding Planner"
  short_description: "为复杂编码任务生成风险自适应、可审批且可验证的持久规划"
  default_prompt: "Use $complex-coding-planner to route by impact and risk, research only decision-changing unknowns, produce a compact profile-aware task bundle, and hand frozen plan-review inputs to $complex-coding-reviewer with the risk-derived dispatch policy before approval."
