interface:
  display_name: "Complex Coding Reviewer"
  short_description: "协调独立 Reviewer 子 Agent，生成目标绑定的正式工程审查证据"
  default_prompt: "Use $complex-coding-reviewer as the review coordinator: freeze the target and context, delegate one isolated reviewer Agent when available, and produce a validated dispatch-bound receipt."
