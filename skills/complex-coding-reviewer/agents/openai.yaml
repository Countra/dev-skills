interface:
  display_name: "Complex Coding Reviewer"
  short_description: "按风险协调 same-context 或独立审查，生成目标绑定证据"
  default_prompt: "Use $complex-coding-reviewer as the review coordinator: freeze the target and context, use same-context for low or medium risk unless independence is required, delegate one isolated reviewer for strict review, and produce a validated dispatch-bound receipt."
