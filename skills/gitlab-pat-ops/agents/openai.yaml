interface:
  display_name: "GitLab PAT Ops"
  short_description: "Compose guarded GitLab resource operations through SKILL_GITLAB_* PAT settings."
  default_prompt: "Use GitLab PAT Ops to compose project, repository, issue, MR, discussion, review, and pipeline operations with guarded writes."
